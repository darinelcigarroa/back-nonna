{
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "echo 'No frontend to serve for API-only project.'"
    },
    "devDependencies": {
        "axios": "^1.7.4",
        "concurrently": "^9.0.1"
    }
}
